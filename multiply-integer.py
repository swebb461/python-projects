def multiply_number():
    """
    :Takes two numbers, multiplies them, and prints the result as a string
    """
    a = input("type a number:")
    b = input("type another:")
    a = int(a)
    b = int(b)
    print(a * b)
multiply_number()
